spring:
  application:
    name: HOTEL-BOOK-REPRICE-PAD
  cloud:
    config:
      enabled: true
      uri: http://${HOST_CONFIG:localhost}:9296/config-server
    #vault:
     # application-name: RSA-KEYS
      #authentication: TOKEN
      #scheme: http
      #kv:
       # enabled: true
  #config:
    #import: classpath:secret.properties